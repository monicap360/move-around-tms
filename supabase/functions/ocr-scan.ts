// Edge Function: ocr-scan
// Receives a ticket image, returns extracted fields
export default async function handler(imageUrl: string) {
  // 1. Run OCR on image
  // 2. Extract all required fields
  // 3. Return structured ticket data
}
