#!/bin/sh

# Fail if forbidden dynamic segments exist at same depth
if find app -type d \( -name "[org]" -o -name "[tenant]" -o -name "[slug]" \) | grep .; then
  echo "Error: Forbidden dynamic segments found"
  exit 1
fi
