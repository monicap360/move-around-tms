DROP YOUR SPREADSHEETS HERE
===========================
Put your carrier list and/or driver list in this folder (.xlsx, .xls, or .csv),
then tell Claude "done" (or the filenames). Any column layout is fine — Claude
auto-detects the columns and imports straight into the database.

Examples:
  carriers.xlsx     -> owner-operator companies
  drivers.xlsx      -> drivers
