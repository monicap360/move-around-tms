import { NextResponse } from 'next/server';

export async function POST() {
  // TODO: Replace with real dispatch assign logic
  return NextResponse.json({ status: 'ok', message: 'Dispatch assign endpoint working.' });
}
